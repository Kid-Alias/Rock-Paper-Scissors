const choices = ['rock','paper','scissors']

const buttons = document.querySelectorAll('.game-btn')

buttons = 

const emojis = {
    rock:     '✊',
    paper:    '✋',
    scissors: '✌️'
}

function updateArena(playerChoice, computerChoice) {

    const playerChoice
}

let scores = {
    user: 0,
    computer: 0,
    tie: 0,
};